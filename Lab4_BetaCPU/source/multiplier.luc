module multiplier #(
    WIDTH = 32
) (
    input a[WIDTH],         // 32-bit input a
    input b[WIDTH],         // 32-bit input b
    output mul[WIDTH]       // 32-bit output mul
) {
    always {
        mul = a*b
    }
}